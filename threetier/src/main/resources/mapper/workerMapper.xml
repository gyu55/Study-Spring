<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.threetier.mapper.WorkerMapper">
    <insert id="insert" parameterType="WorkerVO">
        INSERT INTO TBL_WORKER(ID, WORKER_NAME, WORKER_HOURS_START)
        VALUES(SEQ_WORKER.NEXTVAL, #{workerName}, SYSDATE)
    </insert>

    <update id="update" parameterType="WorkerVO">
        UPDATE TBL_WORKER
        SET WORKER_HOURS_END = SYSDATE
        WHERE ID = #{id}
            AND WORKER_HOURS_END IS NULL
    </update>
</mapper>